import React, { useState } from "react";

const FourSpread = () => {
    const [inputList, setInputList] = useState([{ firstName: "", lastName: "" }])
    // handle input change
    const handleInputChange = (e, ind) => {

    }
    return (
        <div>

        </div>
    )
}

export default FourSpread